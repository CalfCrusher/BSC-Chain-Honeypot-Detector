# Copy this file to .env and fill in your keys
# .env is ignored by git. Do not commit real secrets.

# Preferred Etherscan API v2 key (works for BSC via v2)
ETHERSCAN_API=your_etherscan_api_key_here
# Alternatively, you can use the legacy name
# ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Legacy BscScan v1 API key (optional fallback)
BSCSCAN_API_KEY=your_bscscan_api_key_here

# Optional overrides
# Fixed BNB/USD price to avoid external price lookups
# BNB_PRICE_USD=600

# Optional quick-test addresses (comma-separated)
# HONEYPOT_ADDRS=0x...,0x...
